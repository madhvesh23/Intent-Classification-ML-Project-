# Intent-Based Authentication with Knowledge Graph and LSTM

This project implements a behavioral and contextual intent-based authentication framework using:
- Knowledge graph-style features
- Node2Vec graph embeddings
- LSTM-based sequence model for intent classification
- Rule-based policy guardrail and risk fusion

See `notebooks/` for experiments and `src/` for core implementation.
